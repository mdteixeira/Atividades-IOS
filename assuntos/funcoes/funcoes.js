function myFunc(valor1, valor2) { // Estrutura básica de função
    
    // operações da função
    
    return valor1, valor2; // retorno da função
}

// exemplo prático

let nota1 = 6.5
let nota2 = 7.5

function calcularMedia(nota1, nota2) {
    let media = (nota1 + nota2) / 2
    return media
}

let mediaFinal1 = calcularMedia(nota1, nota2)
console.log(`A média das notas é ${mediaFinal1}`);